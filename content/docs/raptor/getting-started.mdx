---
title: Getting started
description: Prerequisites and local development for Raptor.
---

## Prerequisites

- Bun 1.2+
- Go 1.25+
- Docker (for the local dev infra — Postgres + Redis)

## Local development

```bash
bun install                  # install JS/TS workspaces
bun run dev:up               # start dev infra (Postgres + Redis)

cp apps/panel/.env.example apps/panel/.env          # then fill AUTH_SECRET + ENCRYPTION_KEY
bun run --filter @raptor/panel db:migrate           # apply database migrations

bun run dev                  # run the panel (Vite dev) on :3000
bun run dev:down             # stop the dev infra when done
```

## Building wings

```bash
bun run wings:build          # build the wings binary
bun run wings:run            # run the daemon (needs `wings configure` first)
```

See the [repository](https://github.com/xena-studios/raptor) for the full contributor guide and
the trust model in `.claude/rules/architecture.md`.
